{
  "name": "agent-colony",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A live hex map of the Claude Code projects on your machine: which agents work, which wait for you, and what is still uncommitted",
  "license": "SEE LICENSE IN LICENSE",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "collect": "node src/collect.mjs --print",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@floating-ui/dom": "^1.8.0",
    "@shoelace-style/shoelace": "^2.20.1",
    "three": "^0.186.0"
  }
}
